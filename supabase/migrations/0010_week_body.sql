-- 주에 본문을 준다 (SPEC §2.2).
--
-- 전까지 주는 제목 한 줄이 전부였다. 「전투가 돌아간다」가 무엇으로 확인되는지,
-- 이번 주에 무엇을 안 건드리는지 적을 자리가 없었다. 티켓만 형식이 있고 그 위
-- 두 층은 없어서, 주가 끝났는지를 티켓 개수로만 판단해 왔다.
--
-- 태스크는 이미 description 이 있어 컬럼을 더 만들지 않는다 — 그 칸을
-- 「## 무엇을 / ## 확인」 형식으로 쓰기로만 정했다.
alter table weekly_goals
  add column if not exists body text;
  -- 「## 확인」(체크박스) + 「## 안 하는 것」(선택). critic 이 형식을 검사한다.
