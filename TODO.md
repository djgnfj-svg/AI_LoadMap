# TODO

아직 안 한 일을 적어둔다. 확정된 설계는 `docs/SPEC.md` 로 옮긴다.

## 1. 화면 점검 (UI)

**한 화면씩 스크린샷을 찍어가며 본다.** 지금까지는 기능만 보고 화면을 안 봤다.
사용자가 직접 돌려보며 이상한 곳을 알려주는 중이므로, 아래 목록은 계속 늘어난다.

점검 대상 (`frontend/src/screens`, `frontend/src/components`):

- [ ] `Login` — 로그인 (아직 못 봤다. 데모 로그인 상태라 화면이 안 뜬다)
- [x] `GoalInput` — 첫 로드맵 만들기
- [x] `ProjectList` — 내 로드맵 목록 (빈 상태만 봤다. 카드가 있는 상태는 아직)
- [x] `Main` — 티켓 보드 + 다이어그램 (대비만 봤다. 배치·여백은 아직)
- [ ] `ReviewSession` — 재점검 (재점검일이 있어야 볼 수 있다)
- [ ] 패널들 — `TicketDetail` · `AlertsPanel` 은 아직. 나머지는 메인에서 함께 봤다

대비는 브라우저에서 계산해 확인한다 (WCAG AA: 본문 4.5:1, 큰 글씨 3:1).
점검한 세 화면은 현재 **미달 0**.

### 고친 것 (2026-09-10)

- [x] **첫 로드맵 만들기 버튼 글씨가 안 보였다.** 대비 **1.01:1** — 배경과 사실상 같은 색.
      `button { color: inherit }` 이 `.empty { color: var(--dim) }` 를 물려받아 파란 버튼에
      회색 글씨가 됐다. → `button`·`input`·`textarea` 가 문맥 색을 상속하지 않게 하고
      (`color: var(--text)`), `button.primary` 는 `color: #fff` 를 스스로 갖게 했다. → 4.63:1
- [x] **`--dim` 이 본문에 쓰이기엔 어두웠다** (4.12:1). `.hint` 등 화면 전반이 미달이었다.
      `#6e7681` → `#7d8590` (5.1:1). `--muted`(6.15) 와는 여전히 구별된다.
- [x] 비활성 버튼 `opacity: 0.45` → `0.6`.

### 남은 것

- [ ] **화면이 위쪽에 몰리고 아래가 텅 빈다.** 넓은 화면(2560px)에서 특히 그렇다.
- [ ] **생성 진행 로그(SSE)가 질문 화면 아래 계속 남는다.** 이미 끝난 단계인데 안 사라진다.
- [ ] 다이어그램 우하단 미니맵이 잘려 보인다.

## 2. 생성 흐름을 한 화면에 한 질문으로

- [x] **넣었다** (2026-09-10, `screens/GoalInput.tsx`). 첫 화면이 목표·기간·시간·도구를
      한꺼번에 받던 것을 **네 단계로 쪼갰고**, 서버가 한 묶음으로 주는 인터뷰 질문도
      화면에는 하나씩만 내민다. 진행 표시(`1 / 4`), `← 이전`, 자동 포커스,
      한 줄 입력에서 Enter 로 다음. 마지막 단계에서만 버튼 문구가 바뀐다.
      백엔드는 건드리지 않았다 — 답은 예전처럼 마지막에 한꺼번에 보낸다.

첫 질문은 요청대로 이 문구로 고정했다:

> 당신이 만들고자 하는 것의 청사진이 무엇인가요?

### 물어본 걸 또 묻던 것 — 해결

- [x] 첫 화면이 기간·가용시간·도구를 받고 서버 인터뷰가 그걸 또 묻던 문제.
      **첫 화면이 묻는 것을 줄이는 쪽으로 갔다.** 이제 첫 화면은 도메인과 목표 한 줄만
      받고, 기간(`deadline`)·가용시간(`hours_per_week`)은 인터뷰가 받는다.
- [x] 청사진 중복도 사라졌다. 1번이 「무엇을 만들 건가요」(도메인 선택)이 되면서
      청사진은 인터뷰 1번 한 곳에서만 묻는다.

## 2-1. 무엇을 만드는지를 1번 질문으로 (2026-09-10)

- [x] 도메인을 **게임 제작 · 소프트웨어 · 웹 제작** 셋으로 좁혔다 (`general` 제거,
      마이그레이션 `0009`). 「그 밖의 만들기」는 축이 흐려 노드가 안 그려졌다.
- [x] 1번에서 고른 도메인이 **2번부터의 질문을 가른다.**
      질문 문구를 `graphs/domains.py` 의 `preset.questions` 로 옮겼다 —
      field 는 그대로라 `apply_answers` 와 화면은 안 건드렸다.
- [x] 고른 값이 intake 의 추측을 이긴다 (`plan_graph.py` 의 `state.get("domain") or ...`).
      추론에 맡기면 틀렸을 때 되돌릴 자리가 없다.
- [x] **청사진은 건너뛸 수 없다** (`interview.REQUIRED_FIELDS`). 화면에서 버튼을 막고,
      서버도 빈 답을 「건너뜀」으로 치지 않고 그 질문만 다시 낸다.

## 2-2. 화면 텍스트를 핵심만 남긴다 (2026-09-10)

- [x] 인터뷰 질문 12개, 안내문·힌트 전부 줄였다. 규칙은 `CLAUDE.md` 에 적었다.
- [ ] 아직 안 본 화면의 텍스트 — `Main` · `ReviewSession` · 패널들.

## 3. 고쳐야 할 것 (2026-09-10 로컬 구동 중 발견)

- [ ] **데모 계정이 없으면 로그인이 아예 막힌다.** `demo_login` 은 고정 UUID 행을
      조회만 하고 없으면 만들지 않아 500 이 난다 (`backend/app/api/auth.py:85`).
      그 행은 `0004_auth.sql` 이 심는 것뿐이라, 지워지면 복구 수단이 없다.
      → 없으면 만들도록 바꾼다.

- [ ] **`scripts/setup.sh` 를 이미 있는 DB 에 다시 못 돌린다.** DB 가 있으면 넘어간 뒤
      `0001` 부터 전부 다시 먹이는데 `0001_init.sql` 이 `create table` 이라 거기서 죽는다.
      마이그레이션 이력 테이블이 없어 어디까지 적용됐는지 알 방법도 없다.
      → 이력 테이블을 두거나, 적용 안 된 것만 고르게 한다.

- [ ] **`scripts/setup.sh` 가 `psql` 을 요구한다.** DB 는 도커로 띄우는데 로컬에 psql 이
      없는 환경(현재 개발 PC)에서는 스크립트를 못 쓴다.
      → `docker exec` 경로를 두거나 psql 없이도 되게 한다.

## 3-1. 목업 플래너에서 본 것 (진짜 LLM 으로 다시 봐야 함)

`ANTHROPIC_API_KEY` 가 비어 있어 목업 플래너로 돌린 결과다. **진짜 모델에서도 같은지
확인하기 전에는 버그로 단정하지 않는다.**

- [ ] 도메인을 **게임 제작**으로 골랐는데 구조도 노드가 소프트웨어 낱말로 나왔다
      (API 서버 · 데이터베이스 · 캐시 · 배포). 게임이면 시스템 · 스테이지 · 에셋이어야 한다.
      0008 마이그레이션이 노린 것이 이 지점이다.

## 3-2. LangGraph — 사람 대기를 그래프가 기억하게

- [ ] `compile()` 에 **checkpointer 가 없다** (`plan_graph.py`). 그래서 인터뷰·청사진
      대기마다 `intake` 부터 통째로 다시 돈다. `interview` 노드의 「새로고침 뒤 재실행」
      방어 코드와, 답을 매번 DB 에서 되읽는 것이 그 대가다.
      → `AsyncPostgresSaver` + `interrupt()` 로 바꾸면 멈춘 지점에서 이어진다.
      (기법 이름: Human-in-the-loop + Checkpointing. critic 루프는 Reflection 이다.)
      **지금은 동작하므로 D11 이후로 미룬다.**

## 4. 배포 (D11, 09-17)

배포 방식은 **아직 안 정했다.** `docs/SPEC.md` §3.1 스택 표에 인프라 항목이 없고,
D11 완료 기준은 "공개 URL 동작" 한 줄뿐이다.

- [ ] 무료 호스팅은 그냥은 안 된다. **Render 무료는 15분 무활동 시 프로세스가 잠들어
      APScheduler 가 멈춘다** — 이 제품의 차별점인 실패 감지가 죽는다는 뜻이다.
      외부 크론으로 깨우거나, 감지를 요청 시점에 돌리는 설계가 필요하다.
      (Railway 는 영구 무료 티어가 없다. 가격은 배포 시점에 다시 확인할 것)
- [ ] Docker 로 묶을지 정한다. DB 는 관리형이고 프론트는 정적이라 컨테이너가 필요한 것은
      백엔드 하나뿐이다. `docker-compose` 로 셋을 묶는 그림은 이 구성에 맞지 않는다.

## 5. 개발 환경 (이 PC 한정, 커밋하지 않는 임시 조치)

- [ ] **8000 포트를 정체불명의 프로세스가 쥐고 있다.** Windows·WSL 어디에도 안 보이는데
      옛 코드로 응답한다(`netstat` 이 이미 죽은 PID 를 가리킨다). 그래서 개발 백엔드를
      **8001** 로 띄우고 있다: `BACKEND_URL=http://localhost:8001` 로 vite 를 돌린다.
      재부팅하면 풀릴 것이다.

- [ ] **`site-packages/orjson.py` 는 임시 대체품이다.** 스마트 앱 컨트롤이 orjson 의
      `.pyd` 를 막아 백엔드가 아예 안 뜨기 때문에 표준 json 으로 표면만 맞춰 넣었다.
      실제 패키지는 `orjson.blocked/` 로 비켜뒀다.
      **`uv pip install` 로 의존성을 다시 깔면 날아가고 백엔드가 다시 안 뜬다.**

## 6. LangGraph 고도화 · LangSmith (2026-09-10 검토)

**남은 날 — 접수 09-18 까지 8일, 과제 제출 09-20 까지 10일.** D11(UI 정리 + 배포,
09-17)이 진행 중이고 배포 방식은 §4 대로 아직 안 정했다.

### 6.1 LangChain 도입 — 하지 않는다

이 프로젝트는 **LangChain 을 쓴 적이 없다.** 의존성은 `langgraph` 와 `anthropic`
둘뿐이고, LLM 호출은 `graphs/llm.py` 한 파일에 갇혀 있다
(`messages.parse` + `output_format=` → 파싱된 Pydantic 모델).

`langchain-anthropic` 으로 바꾸면 그 구조화 출력 경로를 통째로 다시 짜야 한다.
`with_structured_output` 은 도구 호출 기반이라 실패 양상이 다르고, critic 이
기대는 스키마 보증이 흔들린다. SPEC §3.1 이 이 스택을 고른 이유가 「구조화 출력
안정성」이었고, R6 은 「새로 배우는 기술 추가 금지」다. **8일 남은 지금 할 일이 아니다.**

바꿔서 얻는 것도 없다 — 모델 교체 여지는 `Planner` 프로토콜이 이미 주고 있다.

### 6.2 LangSmith — 붙인다 (반나절)

LangChain 없이 붙는다. 두 갈래를 각각 켜야 한다.

1. **그래프 실행**: `LANGSMITH_TRACING=true` · `LANGSMITH_API_KEY` 두 환경변수면
   LangGraph 노드가 자동으로 추적된다. 코드 변경 0.
2. **LLM 호출**: 그래프 밖 Anthropic SDK 는 위로 안 잡힌다. `wrap_anthropic` 으로
   클라이언트를 감싸야 프롬프트·토큰·도구 호출이 보인다.

```python
# app/graphs/llm.py — AnthropicPlanner.__init__
from anthropic import AsyncAnthropic
from langsmith import wrappers

self._client = wrappers.wrap_anthropic(AsyncAnthropic(api_key=key))
```

- [ ] `langsmith>=0.3` 를 `pyproject.toml` 에 넣는다 (런타임 의존성).
- [ ] `wrap_anthropic` 감싸기 — `llm.py` 한 줄. 경계 파일 하나만 건드린다.
- [ ] `.env.example` 에 `LANGSMITH_TRACING` · `LANGSMITH_API_KEY` · `LANGSMITH_PROJECT`.
      **셋 다 비워두면 아무 일도 안 일어나야 한다** (목업 Planner · 구글 로그인과 같은 결).
- [ ] 키가 없을 때 죽지 않는지 확인. 테스트는 `FakePlanner` 라 애초에 안 닿지만,
      import 실패는 전부를 세운다.

**왜 쓸 만한가.** 이 그래프의 존재 이유는 critic 재시도 루프인데, 지금은 몇 번
돌았고 무엇 때문에 돌려보냈는지가 로그에만 남는다. 추적을 붙이면 그 루프가 **한
화면에 그림으로 남는다** — 데모 영상(D13)에서 「검증 없이 한 번 호출하는 래퍼가
아니다」를 말이 아니라 증거로 보인다.

**주의.** 프롬프트 원문과 인터뷰 답변이 외부 서비스로 나간다. 시연용 계정 데이터만
넣는다. 가격·무료 한도는 붙이는 날 다시 확인할 것.

참고:
- [Trace Anthropic applications](https://docs.langchain.com/langsmith/trace-anthropic)
- [Trace LangGraph applications](https://docs.langchain.com/langsmith/trace-with-langgraph)
- [`wrap_anthropic` 레퍼런스](https://reference.langchain.com/python/langsmith/wrappers/_anthropic/wrap_anthropic)

### 6.3 LangGraph 고도화 = checkpointer (§3-2)

「고도화」의 본체는 여기다. LangChain 은 애초에 논점이 아니었고(6.1), 남은 실제
부채는 **checkpointer 부재**(§3-2)다 — 사람이 기다리는 두 지점(인터뷰·청사진)마다
`intake` 부터 통째로 다시 돈다. `AsyncPostgresSaver` + `interrupt()` 가 답이다.
다만 지금 동작하므로 순서는 그대로 — **배포(D11) 다음이다.**

## 7. 도메인을 게임 하나로 좁혔다 (2026-09-10)

셋(게임 · 소프트웨어 · 웹)을 다 받으면 셋 다 어설프다. 접수까지 8일이라 하나만 판다.

- [x] **첫 화면의 도메인 선택 카드를 뺐다** (`screens/GoalInput.tsx`). 이제 첫 화면은
      「어떤 게임을 만드시나요?」 하나다. 2단계(`setupStep`)가 사라져 진행 표시와
      `← 이전` 도 같이 뺐다.
- [x] 새 로드맵은 전부 `domain = "game"`. `DOMAINS` · `pick` · `.domain-cards` 는
      쓰는 데가 없어져 지웠다.
- [x] `docs/SPEC.md` §1.5 · `README.md` 「도메인」을 다시 썼다. 둘 다 0009 이전
      낱말(「그 밖의 만들기」)로 남아 있어 코드와 어긋나 있었다.

**지우지 않은 것.** `software` · `web` 프리셋과 DB 제약(`0009`)은 그대로 둔다.
안 내미는 것과 지우는 것은 다르다 — D10 에 넣은 자기 프로젝트(이 로드맵 자신,
`software`)와 그 전에 만든 로드맵이 제 낱말로 계속 읽혀야 한다.
마이그레이션 `0010` 은 만들지 않았다. 되돌리려면 화면 하나만 되돌리면 된다.

확인: `tsc --noEmit` · `vite build` 통과, 백엔드 `pytest` 91 passed / 73 skipped
(건드린 파일이 프론트뿐이다).

### 이제 더 급해진 것

- [ ] **§3-1 의 그 버그.** 게임을 골랐는데 구조도가 소프트웨어 낱말로 나오던 것 —
      이제 게임이 유일한 도메인이라 이게 틀리면 제품 전체가 틀린다.
      `ANTHROPIC_API_KEY` 를 넣고 진짜 모델로 먼저 확인할 것.
- [ ] `seed_self.py` 는 `software` 로 남는다. 데모에서 「게임만 되는 도구인데 시드는
      소프트웨어」로 보일 수 있다. 게임 예시 로드맵을 하나 더 시드할지 정한다.

## 8. 1라운드 질문을 일 셋 + 사정 넷으로 다시 짰다 (2026-09-10)

**앞의 셋이 일을 쪼개고, 뒤의 넷이 사정을 잰다.** 사람 사정은 계획의 **크기**만
정하고 무엇을 할지는 못 정한다. 「몇 명이 몇 시간」을 아무리 정확히 받아도
무엇이 들어가는지 모르면 남의 계획이 나온다. 그래서 일이 먼저다.

| # | | field | 게임 문구 |
|---|---|---|---|
| 1 | 일 | `blueprint` | 당신이 만들고자 하는 게임의 청사진이 무엇인가요? |
| 2 | 일 | `parts` | 이 게임에 무엇무엇이 들어가나요? |
| 3 | 일 | `starting_point` | 지금 어디까지 돼 있나요? |
| 4 | 사정 | `team_size` | 몇 명이 만드나요? |
| 5 | 사정 | `level` | 게임을 만들어 본 적 있나요? |
| 6 | 사정 | `deadline` | 언제까지 만드시나요? |
| 7 | 사정 | `hours_per_week` | 한 주에 몇 시간 쓸 수 있나요? |

- [x] **1번 문구를 §2 에 적어 둔 그대로 되돌렸다.** §2-1 에서 도메인 프리셋으로
      옮기며 「플레이어가 무엇을 하고, 한 판이 어떻게 끝나나요?」로 바뀌어 있었다.
- [x] `team_size` · `level` · `hours_per_week` 를 1라운드로 올렸다.
      **추측이 있어도 묻는다** — `_CRITICAL_FIELDS`(가용시간만 조건부로 묻던 것)를
      지웠다. 틀린 추측은 되돌릴 자리가 없다.
- [x] `done_when` 을 뺐다. 청사진 답과 겹쳤고, 완성 기준은 다음 화면에서 사용자가
      직접 고쳐 확정한다.
- [x] **2번(`parts`)을 넣고, 그 답이 `architect` 까지 가게 했다.** 전에는 문답 전문이
      `decompose` 로만 가고 architect 는 목표 문장과 주·태스크만 봤다 — 그래서
      게임이라고 해도 노드가 「API 서버 · 데이터베이스」로 나왔다(§3-1 이 그거다).
      이제 사용자가 부른 이름이 그대로 노드 label 이 된다.
- [x] `MAX_QUESTIONS_PER_ROUND` 5 → 7. 다섯 칸이 한 화면에 쏟아지던 때의 숫자였다.
- [x] **사람 말로 온 답을 읽는다** (`apply_answers`).
      「저 혼자요」 → 1, 「유니티로 3년 했어요」 → `advanced`, 「몇 개 만들어 봤어요」
      → `intermediate`. 못 읽으면 추론값을 그대로 둔다.
- [x] **숫자는 첫 것 하나만 읽는다.** 자릿수를 이어 붙이던 탓에 「2~3명」이 23명,
      「주 10~15시간」이 1015시간이 됐다. 묻는 질문이 늘어난 만큼 실제로 올 답이다.
- [x] 청사진 칸에 **예시를 미리 적어 뒀다** (`domain.ts` 의 `blueprintPlaceholder`).
      첫 화면이 「한 줄」을 받으므로, 이 칸이 자세히 적는 자리라는 걸 예시로 보인다.

확인: `pytest` 93 passed / 73 skipped, `ruff check app/` 통과, `tsc` · `vite build` 통과.


## 9. 만들다 생긴 일을 받아서 자리를 잡아 준다 (2026-09-10, 넣었다)

**이게 이 도구의 본체다.** 계획을 세워 주는 도구는 많다. 계획대로 안 될 때
「이건 여기서 하고, 저건 저기서 이렇게」를 풀어 주는 것이 이 제품의 주장이다.

전까지 새 티켓이 들어올 문은 **재점검일 하나뿐**이었고, 그 문은 지연이 쌓여야
열렸다(§2.3). 만들다 「아 이것도 해야 하네」가 떠오른 순간에는 넣을 자리가 없었다.

- [x] `POST /projects/{id}/tickets` — 제목 한 줄을 받아 자리를 정한다. **적용은 안 한다.**
- [x] `POST /projects/{id}/tickets/{session_id}/apply` — 항목별 승인/거절.
- [x] 배치 그래프 `graphs/place_graph.py` — `propose(AI) → critic(규칙) → diff`.
      재설계 그래프와 **같은 critic** 을 쓴다. 규칙이 두 벌이 되면 안 된다.
- [x] 화면: 티켓 보드 아래 **「+ 할 일 추가」** (`components/AddTicket.tsx`).
      「4주차 · 기반 세우기 · 「목록 화면 만들기」 앞」 한 줄과 이유를 내밀고,
      체크박스로 항목을 뺄 수 있다.
- [x] 목업 Planner 도 자리를 고른다 — **가장 한가한 주**의 태스크에 넣고 그 태스크의
      안 끝난 티켓 하나를 선행으로 잡는다. 키 없이 도는 데모에서도 보여야 한다.

**새로 만들지 않은 것.** 변경 타입은 재설계와 같은 `add_ticket` · `add_dependency`
둘뿐이라, 적용(`replan_apply.py`)·초안 미리보기(`replan.apply_to_draft`)·critic 을
그대로 쓴다. 세션도 `replan_sessions` 에 `review_day_id = null` 로 남긴다 —
승인 대기 중인 diff 라는 점에서 재설계 세션과 같은 것이다. **새로 낸 건 문뿐이다.**

설계는 `docs/SPEC.md` §3.7 에 적었다.

### 곁다리로 고친 것

- [x] **§3-1 의 그 버그를 목업에서 잡았다.** 게임인데 구조도가 「API 서버 ·
      데이터베이스」로 나오던 것 — 목업 `_ArchitectResult` 가 낱말을 하드코딩하고
      있었다. 이제 인터뷰 2번(`parts`)의 답을 그대로 노드로 쓴다.
      실제로 넷코드 · 전투 · 인벤토리 · 보스 스테이지 · 사운드로 나온다.
      (진짜 모델에서는 §8 에서 architect 에 문답 전문을 넣은 것이 같은 일을 한다.)
- [x] 승인 줄의 체크박스가 한 칸을 통째로 먹던 것 — 전역 `input { width: 100% }`.

### 남은 것

- [ ] **진짜 키로 한 번 돌려본다.** 목업은 내용을 못 읽어서 「가장 한가한 주」로만
      고른다. 자리를 **내용**으로 고르는지는 실제 모델에서만 확인된다.
- [ ] 넣은 티켓이 다이어그램 어느 노드에 붙었는지 화면에서 바로 안 보인다.
- [ ] D11 배포(§4)는 그대로 남았다. 접수 09-18 까지 8일.

## 10. 주차마다 청사진을 이미지·영상으로 남긴다 (아이디어, 지금 안 한다)

**아직 아이디어다.** 지금 손대기엔 무겁다 — D11 배포(§4)가 먼저다.

계획은 글로만 남는다. 「4주차에 뭘 만들기로 했더라」를 다시 읽어야 안다.
주차가 끝날 때 **그 주의 청사진을 그림 한 장(또는 짧은 영상)으로** 남기면
다시 읽지 않아도 보인다. 재점검(§2.3)에서 지난 주와 나란히 놓고 볼 수도 있다.

### 정해야 할 것

- **전체 청사진도 이미지·영상으로 나오나** — 아직 모른다. 주차는 범위가 좁아
  한 장에 담기지만, 전체는 이미 다이어그램이 그 자리를 맡고 있다.
  전체는 **주차 장면을 이어 붙인 영상**이 될 수도 있다 (타임랩스).
- 무엇을 그리나 — 그 주의 티켓? 다이어그램에서 그 주에 닿은 노드?
- 만드는 값이 있나 — 이미지 생성은 느리고 비싸다. 다이어그램을 그대로
  캡처(SVG→PNG)하는 쪽이 싸고 정확할 수 있다. **거기부터 재 본다.**

## 12. 주·태스크에도 본문 형식을 줬다 (2026-09-12, 넣었다)

전까지 형식이 있는 건 티켓뿐이었다. 주는 제목 한 줄, 태스크는 자유 서술.
그러면 **주가 끝났는지를 티켓 개수로만 판단하게 된다** — 티켓을 다 닫았는데 그 주의
목표는 안 선 경우를 잡을 자리가 없었다.

층마다 맡는 일이 달라 형식도 다르게 갔다. 제목이 「무엇이 있는가」면 본문은
「그걸 어떻게 확인하는가」다. 제목에 쓴 것을 본문에서 되풀이하지 않는다.

| 층 | 확인 항목 최소 | 절 |
|---|---|---|
| 주 | 2개 | `확인` (필수) · `안 하는 것` (선택) |
| 태스크 | 1개 | `무엇을` · `확인` |
| 티켓 | 2개 | `무엇을` · `완료 조건` · `참고` (전과 같다) |

- [x] `0010_week_body.sql` — `weekly_goals.body`. 태스크는 이미 `description` 이 있어
      컬럼을 더 만들지 않았다. 그 칸을 형식대로 쓰기로만 정했다.
- [x] **검사를 한 자리로 모았다.** 세 층이 같은 파서·같은 「잘 동작한다」 필터를 쓴다.
      전에 티켓만 보던 자리를 표 하나로 돌게 바꿨다 (`critic.py` §6).
      최소 개수 상수는 `schemas.py` 로 옮겼다 — 요구하는 쪽(프롬프트)과 검사하는
      쪽(critic)이 같은 값을 봐야 한다.
- [x] `repair` 도 세 층을 채운다. 멀쩡한 항목은 남기고 모자란 만큼만 지어내며,
      `(확인 필요)` 를 붙여 지어낸 티를 남긴다. 「안 하는 것」 절은 건드리지 않는다.
- [x] 화면 — 주·태스크 제목 아래 접힌 줄 (`components/BodyNote.tsx`). 펴면 확인 항목이
      보인다. 보드의 주인공은 티켓이라 기본은 접어 둔다.
- [x] 목업도 형식대로 낸다. 키 없이 도는 데모에서도 보여야 한다.

**재설계·투입 그래프는 이 검사를 끈다** (`check_bodies=False`). 그 초안에는 규칙이
생기기 전에 저장된 주·태스크가 실려 있고, 그건 이번 변경이 고칠 대상이 아니다.
이미 티켓 때문에 있던 장치라 새로 만든 것이 없다.

설계는 `docs/SPEC.md` §2.2 에 적었다 (제목을 「티켓 설계」에서 「본문 형식」으로 바꿨다).

### 남은 것

- [ ] 진짜 키로 한 번 돌려본다. **목업은 형식을 지킬 수밖에 없다** — 하드코딩이라
      그렇다. 실제 모델이 「안 하는 것」에 쓸 말을 찾는지는 거기서만 확인된다.
- [ ] 본문이 늘어난 만큼 decompose 출력도 커졌다. §11-1 의 `max_tokens` 한계가
      그만큼 앞당겨진다 — 주를 쪼개 부르는 일이 더 급해졌다.

## 13. 토큰을 어디서 줄일까 (2026-09-12, 실측)

목업으로 12주 로드맵 한 번을 뽑으며 호출마다 입력·출력 문자수를 쟀다.
토큰은 한글 JSON 기준 **1.6자/토큰**으로 환산했고, 단가는 sonnet-5 ($2 / $10 per MTok).

### 잰 것

| 호출 | 입력(자) | 출력(자) | 비용 | 몫 |
|---|---:|---:|---:|---:|
| intake ×3 | 2,601 | 495 | $0.0063 | 2% |
| clarify ×2 · blueprint | 3,581 | 215 | $0.0058 | 2% |
| **decompose #1** | 2,352 | **17,893** | $0.1148 | **33%** |
| architect #1 · link #1 | 7,846 | 3,713 | $0.0330 | 10% |
| **decompose #2 (재시도)** | 6,462 | **21,900** | $0.1450 | **42%** |
| architect #2 · link #2 (재시도) | 9,173 | 4,504 | $0.0396 | 11% |
| **합계** | **32,015** | **48,720** | **$0.345** | |

**세 가지가 바로 보인다.**

1. **비용의 88%가 출력이다** (입력 20,009토큰 $0.040 / 출력 30,450토큰 $0.305).
   출력 단가가 5배라 그렇다. **입력을 아무리 줄여도 손대는 최대치가 12%다.**
2. **출력의 82%가 decompose** 두 번이다 (39,793 / 48,720자).
3. **재시도 한 번이 전체의 54%** 다. critic 이 걸면 decompose·architect·link 가
   통째로 다시 돈다.

### ⚠ 앞서 §11-3 에 적은 캐싱 이야기는 틀렸다

「같은 인터뷰 전문이 12번 흐르는데 `cache_control` 이 없다」고 적었지만, **지금 구조로는
붙여도 안 걸린다.** 두 가지 이유다.

- **시스템 프롬프트가 376자 ≈ 235토큰이다.** 캐시 최소 프리픽스가 sonnet-5 는
  1024토큰, haiku-4.5 는 4096토큰이다. **미달이면 조용히 캐시가 안 된다** — 에러도 안 난다.
  모델을 낮출수록 더 안 걸린다 (§11).
- 캐싱은 **프리픽스 일치**다. 단계마다 프롬프트 틀이 달라서 (`INTAKE` · `DECOMPOSE` ·
  `ARCHITECT`) 인터뷰 전문이 여러 번 흐르긴 해도 **앞머리가 달라 일치하지 않는다.**
  이건 대화가 누적되는 에이전트 루프가 아니라 일회성 파이프라인이다.

캐싱이 가장 큰 레버라는 건 누적 루프 이야기고, **이 워크로드에는 해당하지 않는다.**
붙이려면 프롬프트를 「공통 앞머리 + 단계별 꼬리」로 다시 배치해야 한다 (아래 3번).

### 줄일 수 있는 것 — 절감 큰 순서

**1. 재시도가 전부를 다시 뱉는 것을 막는다 (≈ -32%)**

`DECOMPOSE_RETRY` 는 이미 「걸린 부분만 고친다」고 적어 뒀다. 그런데 **출력 스키마가
`DecomposeResult` 전체**라 모델은 안 고친 티켓 90개를 다시 써야 한다. 실제로 재시도
출력(21,900자)이 첫 출력(17,893자)보다 **크다.** 지시는 사고만 줄이지 출력을 못 줄인다.

→ 재시도 출력을 **패치 스키마**로 바꾼다 (바뀐 주·태스크·티켓만). 위반이 티켓 10개면
출력이 21,900 → 4,000자쯤이 된다.

**2. 재시도에 architect 를 딸려 보내지 않는다 (≈ -4%)**

그래프가 `decompose → architect → link` 무조건 직렬이라, 티켓 하나를 쪼갠 재시도에도
구조도를 처음부터 다시 그린다. **노드가 바뀌는 위반은 셋뿐이다** —
`orphan_node` · `uncovered_criterion` · `empty_plan`.
120분 초과 · 주간 초과 · 본문 부실 · 순환은 노드를 안 건드린다.

→ 위반 코드를 보고 architect 를 건너뛴다. (link 는 새 티켓 키 때문에 다시 돌아야 한다.)

**3. 프롬프트를 「공통 앞머리 + 단계별 꼬리」로 재배치하고 캐싱을 켠다 (≈ -6%)**

앞머리(시스템 + 목표 + 인터뷰 전문 + 청사진)를 모든 단계가 같은 바이트로 공유하게
만들면 그때부터 캐시가 걸린다. decompose 프리픽스만 해도 1,976자 ≈ 1,235토큰으로
sonnet-5 최소(1024)를 넘는다. 입력이 12%뿐이라 천장이 낮지만 **품질을 안 깎는다.**

⚠ 앞머리에 시각·난수를 넣으면 안 된다. 한 바이트만 달라도 뒤가 전부 무효가 된다.

**4. intake 가 라운드마다 다시 도는 것 (≈ -1.5%)**

인터뷰 라운드마다 `IntakeResult` 를 다시 뽑는다. 같은 목표 문장에서 같은 답이 나온다.
→ 첫 라운드 결과를 상태에 들고 간다.

**5. 모델·effort — 마지막이다.** §11-2 (haiku 는 `thinking: adaptive` 를 안 받는다)가 먼저다.

### §11-1 의 「쪼개면 비용도 준다」도 정정한다

주 단위로 나눠 부르면 **앞머리가 12번 반복돼 입력이 +13,585토큰(+8%) 는다.**
비용만 보면 손해다. 다만 셋을 같이 하면 이야기가 달라진다:

| | 비용 |
|---|---|
| 지금 | $0.345 |
| 주 단위 분할만 | $0.372 (+8%) |
| + 공통 앞머리·캐싱 (3번) | $0.348 |
| + 재시도가 한 주치만 (1번이 저절로 풀린다) | **≈ $0.22 (-36%)** |

**주 단위로 쪼개면 재시도도 한 주치만 다시 내게 된다.** 1번을 따로 안 만들어도 된다는
뜻이다. `max_tokens` 한계(§11-1)와 판단 범위까지 한 번에 풀린다 — **분할 + 앞머리 통일이
하나의 일이다.**

### 먼저 할 것이 아닌 것

- 티켓 본문을 줄이는 것. 출력의 대부분이지만 **그게 제품이다.**
- `max_tokens` 를 낮추는 것. 모델이 못 보는 값이라 중간에 잘릴 뿐 아껴지지 않는다.
- 배치 API (50% 할인). 사용자가 생성 화면에서 기다리는 중이라 못 쓴다.

## 14. 배포 방식을 정한다 (2026-09-12)

§4 가 「아직 안 정했다」로 남아 있던 자리다. 코드를 보고 제약부터 확정했다.

### 제약 — 서버리스는 안 된다

- **SSE.** `GET /projects/{id}/stream` 이 `EventSourceResponse` 로 생성 진행을 흘린다
  (`api/projects.py:77`). 생성 한 번이 **몇 분간 연결을 잡는다.** 함수형 호스팅의
  실행 시간 제한·응답 버퍼링과 맞지 않는다.
- **APScheduler 가 프로세스 안에서 돈다** (`scheduler.py`). 매일 00:10 · 00:20 · 09:00,
  매주 일 20:00. **잠드는 무료 티어에서는 실패 감지가 죽는다** (§4 가 이미 지적).

→ **잠들지 않는 지속 프로세스가 필요하다.** 크론을 밖으로 빼는 설계도 가능하지만
(작업 넷이 전부 SQL 이고 `today` 를 인자로 받아 HTTP 로 부를 수 있다),
**잠들지 않는 가장 싼 기계를 사는 것이 그 엔지니어링보다 싸다.**

### 정한 것 — 컨테이너 하나, URL 하나

**백엔드가 빌드된 프론트 정적 파일까지 서빙한다.** §4 가 이미 「컨테이너가 필요한 것은
백엔드 하나뿐」이라고 적었는데, 한 걸음 더 가면 프론트 호스팅도 필요 없다.
같은 오리진이 되면서 **한꺼번에 사라지는 것들:**

- `CORS_ORIGINS` 설정과 `CORSMiddleware` (`main.py:78`)
- 쿠키 `SameSite` · `Secure` 조합 문제
- 구글 OAuth 승인 오리진·리다이렉트를 두 곳에 맞추는 일
- `BACKEND_URL` 로 vite 를 가리키는 개발/배포 차이 (§5)

DB 는 Supabase 관리형 그대로. 가격은 배포 시점에 다시 확인한다.

### 배포 전에 막히는 것

1. **마이그레이션 이력 테이블이 없다** (§3). 새 DB 는 0001~0010 을 순서대로 먹이면
   되지만, **다음 배포에서 0011 을 더하면 `setup.sh` 가 0001 의 `create table` 에서
   죽는다.** 배포 직후 바로 걸리는 문제라 이게 1번이다.
2. `SESSION_SECRET` 을 반드시 채운다 — 비우면 재시작마다 로그인이 풀린다.
   `SESSION_COOKIE_SECURE=true` (https).
3. **로그인 방식을 고른다.** `GOOGLE_CLIENT_ID` 가 들어오면 **데모 문이 닫힌다.**
   혼자 쓸 거면 데모가 빠르지만 §3 의 500 버그를 먼저 고쳐야 한다.
   구글을 붙이면 그 버그는 무관해진다.
4. `ANTHROPIC_API_KEY` — 한 번에 $0.15~0.20 (§13).

### 쓰면서 조정하려면 먼저 있어야 하는 것

**이게 배포 자체보다 중요하다.** 없으면 이상한 결과를 봐도 왜 그런지 알 수 없다.

1. **`response.usage` 를 로깅한다.** 지금 `llm.py` 가 usage 를 통째로 버린다.
   **§13 의 숫자가 전부 추정인 이유가 이것이다.** 한 줄 로그면 실측이 된다.
   입력·출력·사고 토큰이 따로 나오니 §13 을 다시 쓸 수 있다.
2. **프롬프트와 응답을 남긴다.** 저장하는 테이블이 없다 (`replan_sessions` 는 diff 만
   들고 있다). 티켓이 이상하게 나왔을 때 **그걸 만든 프롬프트를 볼 수 없다.**
   조정은 그 둘을 나란히 놓고 하는 일이다.
   → LangSmith (§6) 가 정확히 이 자리다. 환경변수 둘이면 붙는다.
3. §1 의 남은 UI 셋 — 화면이 위로 몰림 · SSE 로그가 안 사라짐 · 미니맵 잘림.
   직접 쓰면 바로 걸리는 것들이다.

### 순서

`usage 로깅` → `마이그레이션 이력` → `정적 서빙 + Dockerfile` → 배포 → `LangSmith` → UI 셋

앞의 둘은 작고, 배포 뒤에 하면 값을 잃는다 (첫 실사용 데이터를 못 남긴다).

### 무엇으로 배포할까 — Railway Hobby ($5/월), 백엔드와 DB 를 한 프로젝트에

2026-09-12 에 가격과 조건을 다시 확인했다. §4 가 적어 둔 판단 근거가 **바뀌었다.**

| | 지금 조건 |
|---|---|
| **Fly.io** | **신규 무료 티어가 없어졌다.** shared-cpu-1x 512MB ≈ $3.32/월, 1GB ≈ $5.92/월 |
| **Render** | 무료는 **15분** 무활동 시 잠든다 (30분에서 줄었다). 항상 켜짐 Starter **$7/월**. 무료 Postgres 는 30일 후 만료 |
| **Railway** | Hobby **$5/월** ($5 크레딧 포함, 초과분 별도). 이 규모면 크레딧 안에서 끝난다 |
| **Supabase** | 무료: 500MB · 2 프로젝트 · **7일 무활동 시 프로젝트 일시정지.** 유료(Pro $25/월)는 안 멈춘다 |

**결정을 가른 것은 호스팅 가격이 아니라 Supabase 무료의 7일 정지다.**
혼자 띄엄띄엄 쓰면서 조정하는 용도라, 일주일 안 만지면 DB 가 멈춰 있다.
스케줄러가 매일 쿼리하니 살아남을 수도 있지만 「충분한 사용자 활동」 기준이 불명확해
보장이 아니다. 깨우는 장치를 또 만드는 건 §4 가 피하려던 바로 그 일이다.

→ **Railway Hobby 하나에 백엔드 컨테이너와 Postgres 를 같이 올린다.**

1. DB 가 안 멈춘다 — 7일 정지 문제가 사라진다.
2. 컨테이너가 안 잠든다 — APScheduler 가 산다 (§4 의 원래 걱정).
3. **갈아끼우는 비용이 0 이다.** 스키마가 순수 Postgres 다 (`pgcrypto` ·
   `gen_random_uuid`). `0001` 의 `auth.users` FK 는 **없으면 건너뛰도록 이미 짜여 있고**,
   `0004` 부터는 자체 `users` 테이블을 든다. Supabase 기능을 쓰는 곳이 없다.
4. Dockerfile 하나 + Postgres 애드온. §14 의 「컨테이너 하나, URL 하나」와 맞는다.

`DATABASE_URL` 만 Railway Postgres 로 바꾸면 된다. `supabase/migrations/` 폴더 이름은
그대로 둔다 — 내용이 순수 SQL 이라 이름만 역사다.

**더 싸게 가려면** Fly.io 512MB($3.32) + Fly Postgres 가 있다. $1.7 차이에 손이 더 가고,
Fly 는 신규 무료가 없어져 무료로 시작하는 길도 아니다. 이 단계에서는 단순한 쪽이 낫다.

가격은 배포 직전에 한 번 더 확인한다 — 이 표도 오늘 값이다.
